<?xml version="1.0" encoding="utf-8"?>
<Patch>
  <!-- The brewery uses the wooden Medieval Overhaul art (see replace_textures.xml),
       which ships with masks, so make it stuffable from wood like the rest of the
       medieval woodwork. The 120 WoodLog from the vanilla costList becomes the
       stuff cost; the Steel stays as a flat cost for the fittings. -->

  <Operation Class="PatchOperationRemove">
    <xpath>Defs/ThingDef[defName="Brewery"]/costList/WoodLog</xpath>
  </Operation>

  <!-- Vanilla sets <constructEffect>ConstructWood</constructEffect>, which RimWorld flags as
       a config error once the def is stuffable (the stuff's own construct animation always
       wins). Dropping it silences that on Brewery and on the generated Frame_Brewery. -->
  <Operation Class="PatchOperationRemove">
    <xpath>Defs/ThingDef[defName="Brewery"]/constructEffect</xpath>
  </Operation>

  <Operation Class="PatchOperationAdd">
    <xpath>Defs/ThingDef[defName="Brewery"]</xpath>
    <value>
      <stuffCategories>
        <li>Woody</li>
      </stuffCategories>
      <costStuffCount>120</costStuffCount>
    </value>
  </Operation>
</Patch>
